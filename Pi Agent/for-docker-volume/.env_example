# NOTE. this file has to use LF, not CRLF !!
GITHUB_AGENT_ACCOUNT=your Agent GitHub account name    (used to show the author of the commit in GitHub)
GITHUB_ORG=AI-Team-26
GITHUB_REVIEWER=your main GitHub account (or a dedicated agent?)

# "ENV:" means it has to be read from environment variable
GITHUB_AGENT_ACCOUNT_PAT=ENV:PI_AGENT_GITHUB_ACCOUNT_PAT
GITHUB_ORG_PAT=github_pat_123456

# For access repositories of main Account
GITHUB_MAIN_ACCOUNT=my-github-account
GITHUB_MAIN_ACCOUNT_EMAIL=john.doe@email.com   (used to define the contributor of the commit in GitHub)
GITHUB_MAIN_ACCOUNT_PAT=ENV:PERSONAL_GITHUB_ACCOUNT_PAT_FOR_PI_AGENT

EXA_API_KEY=